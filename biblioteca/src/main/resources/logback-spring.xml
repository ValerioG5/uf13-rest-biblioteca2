<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Import Spring Boot default logging configuration -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Property to determine active profile -->
    <springProperty scope="context" name="LOG_FILE" source="logging.file.name" defaultValue="/var/log/app/biblioteca.log"/>

    <!-- ================================
         APPENDER: Console Output
         ================================ -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- Dev pattern: detailed with all info -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ================================
         APPENDER: File Output (Rotating)
         ================================ -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>

        <!-- Rolling policy: rotate by day and size -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- ================================
         LOGGER CONFIGURATION
         ================================ -->

    <!-- Application package - detailed logging -->
    <logger name="it.marconi.biblioteca" level="TRACE"/>

    <!-- Hibernate SQL logging -->
    <logger name="org.hibernate.SQL" level="DEBUG"/>
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE"/>

    <!-- Spring Framework -->
    <logger name="org.springframework.web" level="DEBUG"/>
    <logger name="org.springframework.security" level="DEBUG"/>

    <!-- Database connection pooling -->
    <logger name="com.zaxxer.hikari" level="INFO"/>

    <!-- ================================
         PROFILES
         ================================ -->

    <!-- DEV Profile: Console only, TRACE level -->
    <springProfile name="dev">
        <logger name="root" level="DEBUG"/>
        <logger name="it.marconi.biblioteca" level="TRACE"/>
        <logger name="org.hibernate.SQL" level="DEBUG"/>
        <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE"/>
        <logger name="org.springframework.web" level="DEBUG"/>
        <logger name="org.springframework.security" level="DEBUG"/>

        <root level="DEBUG">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!-- PROD Profile: File output, INFO level -->
    <springProfile name="prod">
        <logger name="root" level="INFO"/>
        <logger name="it.marconi.biblioteca" level="INFO"/>
        <logger name="org.hibernate.SQL" level="WARN"/>
        <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="WARN"/>
        <logger name="org.springframework.web" level="WARN"/>
        <logger name="org.springframework.security" level="WARN"/>

        <root level="INFO">
            <appender-ref ref="FILE"/>
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!-- DEFAULT Profile: Console, INFO level -->
    <springProfile name="!dev,!prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

</configuration>
